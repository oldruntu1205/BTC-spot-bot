# BTC Spot Bot — V1 Enterprise 依赖清单
# Python 3.11+

# ── 核心科学计算 ──────────────────────────
numpy>=1.26.0

# ── 配置管理 ──────────────────────────────
pydantic>=2.6.0
pydantic-settings>=2.1.0
pyyaml>=6.0

# ── 日志 (结构化) ─────────────────────────
loguru>=0.7.0

# ── 数据库 (SQLAlchemy 2.x) ──────────────
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# ── 异步 HTTP (币安 REST API) ────────────
aiohttp>=3.9.0

# ── WebSocket (币安实时行情流) ────────────
websockets>=12.0

# ── 多交易所支持 (可选) ───────────────────
# ccxt>=4.4.0

# ── AI 参数优化 (可选) ───────────────────
# optuna>=3.5.0
